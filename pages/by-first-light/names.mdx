import { BasePage } from "../../components/base-page";
import { ByFirstLightIndex } from "../../components/bfl-index";

<div>

The <b>birth name index</b> of <b>By First Light</b> contains the names of over
2,400 descendents of the Bauldauf family.

<ByFirstLightIndex />

</div>

export default BasePage;
